# JWT Configuration
JWT_SECRET=
JWT_ACCESS_TOKEN_EXPIRY_HOURS=8
JWT_REFRESH_TOKEN_EXPIRY_DAYS=7

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Port Configuration
PORT_CODE=PTLEI

# API and Frontend URLs (change these for different environments)
API_URL=http://localhost:5174
FRONTEND_URL=http://localhost:4200

# CORS Configuration (will use FRONTEND_URL if CORS_ALLOWED_ORIGINS not set)
CORS_ALLOWED_ORIGINS=http://vm.nunoepteixeira.me

# Database Configuration (if needed for production)
# DATABASE_CONNECTION_STRING=

# Logging Configuration
LOG_LEVEL=Information

# Token Refresh Configuration
TOKEN_REFRESH_BUFFER_MINUTES=5
